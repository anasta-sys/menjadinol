'use client';import {FormEvent,useState} from 'react';export default function Page(){const [msg,setMsg]=useState('');async function go(e:FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);const r=await fetch('/api/bootstrap-superadmin',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(fd))});const j=await r.json();setMsg(j.message||'')}return <div className="auth"><section className="authVisual"><img src="/menjadi-nol-symbol.png"/></section><section className="authCard"><form className="form" onSubmit={go}><div className="eyebrow">ONE-TIME SETUP</div><h1>Bootstrap Superadmin</h1><p>Gunakan hanya saat instalasi awal. Setelah berhasil, hapus SUPERADMIN_BOOTSTRAP_SECRET dari environment.</p><label>Nama</label><input name="name" required/><label>Email</label><input name="email" type="email" required/><label>Password awal</label><input name="password" type="password" minLength={12} required/><label>Bootstrap secret</label><input name="bootstrap_secret" type="password" required/><button className="btn">Buat Superadmin</button><p>{msg}</p></form></section></div>}
