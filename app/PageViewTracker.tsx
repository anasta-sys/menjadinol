'use client';import {useEffect} from 'react';import {usePathname} from 'next/navigation';export default function PageViewTracker(){const p=usePathname();useEffect(()=>{if(!p||p.startsWith('/admin')||p.startsWith('/writer'))return;fetch('/api/page-view',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({path:p}),keepalive:true}).catch(()=>{})},[p]);return null}
