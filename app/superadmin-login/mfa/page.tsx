import SuperAdminMfa from "./SuperAdminMfa";

export const metadata = {
  title: "Verifikasi Super Admin · kembali ke nol",
  robots: {
    index: false,
    follow: false,
  },
};

export default function SuperAdminMfaPage() {
  return <SuperAdminMfa />;
}